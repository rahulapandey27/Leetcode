class Solution {
public:
    int numPairsDivisibleBy60(vector<int>& time) {
        long res = 0;
        int cnt[60] = {0};
        for (int t : time) {
            int r = t % 60;
            res += cnt[(60 - r) % 60];
            ++cnt[r];
        }
        return (int)res;
    }
};
